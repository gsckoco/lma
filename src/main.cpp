#include "main.h"

#include <bitset>
#include <iostream>
#include <thread>

//void guiThread(OutputWindow *window) {
//    window->init();
//}

int main() {
    OutputWindow window;
    window.init();
}